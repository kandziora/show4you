export class Show {
    id      : number;
    name    : string;
    genre   : string;
    support : string;


    constructor (id:number, name:string, genre:string, support:string){
        this.id         = id;
        this.name       = name;
        this.genre      = genre;
        this.support    = support
    }
}
